{
  "name": "sierx",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted work tracker. Root manifest pins the Node toolchain (BUILD §1); the frontend lives under /web from phase 2.",
  "license": "Apache-2.0",
  "repository": "github:siercks/sierx",
  "engines": {
    "node": ">=24 <25"
  }
}
